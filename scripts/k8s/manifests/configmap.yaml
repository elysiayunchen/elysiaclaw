apiVersion: v1
kind: ConfigMap
metadata:
  name: elysiaclaw-config
  labels:
    app: elysiaclaw
data:
  elysiaclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.elysiaclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "ElysiaClaw Assistant",
            "workspace": "~/.elysiaclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # ElysiaClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
