apiVersion: v1
kind: Service
metadata:
  name: elysiaclaw
  labels:
    app: elysiaclaw
spec:
  type: ClusterIP
  selector:
    app: elysiaclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
