---
summary: "CLI reference for `elysiaclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `elysiaclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `elysiaclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `elysiaclaw clawbot qr` (same behavior as [`elysiaclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `elysiaclaw clawbot qr` -> `elysiaclaw qr`
